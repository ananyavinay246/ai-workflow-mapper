# AI Workflow Mapper Example Report

This is a placeholder-format example report that shows where human-readable outputs belong. Replace with a domain-specific output once implementation fixtures are available.

## Summary

The final report must be generated from validated output data and must not contain uncited claims when the domain requires citations.
